pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Example: Compile code
                // sh 'make build'
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                // Example: Run tests
                // sh 'make test'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
                // Example: Deploy app
                // sh 'make deploy'
            }
        }
    }
}
